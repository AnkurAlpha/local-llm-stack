id: fetch
enabled: true
endpoint: http://mcp-tools:8004/mcp
transport: streamable-http
category_hint: internet
description: Fetch and extract readable content from web pages.
runtime:
  mode: supergateway
  port: 8004
  command:
    - mcp-server-fetch
