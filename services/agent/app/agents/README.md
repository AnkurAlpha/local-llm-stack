# Future agent implementations

Prototype V1 intentionally leaves this package empty. Later versions can add the router, planner,
researcher, mathematics, language-specific coding, test, build, document, and internet-search
agents behind interfaces that depend on `ChatProvider` and `MCPClient`.

