[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["live: requires internet access"]
addopts = "-m 'not live'"

[tool.ruff]
line-length = 110
target-version = "py312"
extend-exclude = ["data"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

